"""
LLM extraction prompt template for {{ domain }} data.

This module contains the prompt template used to instruct Claude Sonnet 4.5
on how to extract structured {{ domain }} data from SEC filings.

Design Decisions:
- **Explicit Examples**: Show concrete input/output pairs
- **Parsing Rules**: Clear instructions for edge cases
- **JSON Schema**: Define expected structure precisely
- **Error Prevention**: Address common LLM mistakes

Usage:
    >>> prompt = build_prompt(
    ...     section_html="<html>...</html>",
    ...     company_name="Apple Inc.",
    ...     cik="0000320193",
    ...     ticker="AAPL"
    ... )
"""

import json
from typing import Optional


def build_extraction_prompt(
    section_html: str,
    company_name: str,
    cik: str,
    ticker: Optional[str] = None,
) -> str:
    """
    Build LLM prompt for {{ domain }} extraction with clear structure.

    Args:
        section_html: Extracted section HTML
        company_name: Company name
        cik: CIK number
        ticker: Stock ticker (optional)

    Returns:
        Complete prompt text
    """
    # Define JSON schema with type annotations
    schema = {{ json_schema | tojson(indent=4) }}

    schema_json = json.dumps(schema, indent=2)

    # Concrete example
    example_input = """{{ example_input }}"""

    example_output = {{ example_output | tojson(indent=4) }}

    example_json = json.dumps(example_output, indent=2)

    prompt = f"""# Task: Extract {{ task_description }}

## Company Information
- **Name**: {company_name}
- **CIK**: {cik}
- **Ticker**: {ticker or 'UNKNOWN'}

## Your Task
{{ task_instructions }}

## Critical Parsing Rules

{% for rule in parsing_rules %}
### {{ loop.index }}. {{ rule.title }}
{{ rule.description }}
{% if rule.examples %}

**Examples:**
{% for example in rule.examples %}
- {{ example }}
{% endfor %}
{% endif %}
{% endfor %}

## Example Extraction

**Input HTML:**
```html
{example_input}
```

**Output JSON:**
```json
{example_json}
```

{% if example_explanation %}
**Explanation:**
{{ example_explanation }}
{% endif %}

## JSON Schema

Return JSON matching this structure:

```json
{schema_json}
```

## HTML Content to Extract

{section_html}

## Output Requirements

**CRITICAL**:
{% for req in output_requirements %}
{{ loop.index }}. {{ req }}
{% endfor %}

Extract the data now.
"""

    return prompt


# Alternative: Pre-built prompt templates for common scenarios
PROMPT_TEMPLATES = {
    "standard": build_extraction_prompt,
    # Add more variations as needed
}


def get_prompt_template(template_name: str = "standard"):
    """
    Get a specific prompt template by name.

    Args:
        template_name: Name of template to retrieve

    Returns:
        Prompt building function

    Example:
        >>> builder = get_prompt_template("standard")
        >>> prompt = builder(section_html="...", company_name="Apple Inc.", ...)
    """
    return PROMPT_TEMPLATES.get(template_name, build_extraction_prompt)
